# Frontend environment variables
# Base URL for API calls. Use your Express server origin for real backend integration.
# Example: http://localhost:4000 or https://api.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Optional: Application name used in the UI
NEXT_PUBLIC_APP_NAME=Flex Living

# Optional: Client-side log level (info | warn | error)
NEXT_PUBLIC_LOG_LEVEL=info